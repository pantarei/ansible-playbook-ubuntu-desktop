---

- hosts: all
  remote_user: root
  tasks:
    - name: apt-add-repository
      apt_repository:
        repo: "{{ item.repo }}"
        state: "{{ item.state }}"
      with_items:
        - { state: "present", repo: "ppa:communitheme/ppa" }

    - name: apt install
      apt:
        name: "{{ item.name }}"
        state: "{{ item.state }}"
      with_items:
        - { state: "latest", name: "communitheme-sounds" }
        - { state: "latest", name: "gnome-shell-communitheme" }
        - { state: "latest", name: "gtk-communitheme" }
        - { state: "latest", name: "suru-icon-theme" }
        - { state: "latest", name: "ubuntu-communitheme-session" }
